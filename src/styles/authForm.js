import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  form: {
    borderWidth: 1,
    borderColor: '#DDD',
    width: 300,
    padding: 10,
    borderRadius: 4,
    top: -50
  },
  button: {
    alignSelf: 'center',
    marginTop: 10
  }
});

export default styles;